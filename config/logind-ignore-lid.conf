[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
HandleSuspendKey=ignore
HandleHibernateKey=ignore
IdleAction=ignore
LidSwitchIgnoreInhibited=no
# Power key still shuts down. Do not ignore it — this box has no
# working local display once stealth is on, so a held power button
# is the last on-chassis stop.
