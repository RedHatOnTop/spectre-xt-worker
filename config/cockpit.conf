[WebService]
# Cockpit listens on all interfaces by default. Pair this file with
# config/cockpit.socket.d/override.conf.example after `tailscale up`.
ProtocolHeader = X-Forwarded-Proto
AllowUnencrypted = false
