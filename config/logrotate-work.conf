# Proxy logs already outgrew their disk once. Rotate weekly, cap total.
/work/logs/*.log {
    weekly
    rotate 4
    size 50M
    maxsize 100M
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
}
